# Copie para .env.local e preencha. .env.local NUNCA vai pro Git.
# Variáveis com NEXT_PUBLIC_ são expostas ao browser — só coisas públicas aqui.

# Supabase (público — frontend)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase (SERVIDOR APENAS — nunca prefixar com NEXT_PUBLIC_)
SUPABASE_SERVICE_ROLE_KEY=

# OpenAI Whisper (M2 — servidor apenas)
# OPENAI_API_KEY=
